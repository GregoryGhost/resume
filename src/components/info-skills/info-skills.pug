include ../progress-bar/progress-bar.pug
include ../info/info.pug

mixin info-skills(title, skills, maxSkillPoint)
    mixin skills(data)
        .info-skills
            each skill, label in data
                +progress-bar(label, skill, maxSkillPoint)
    +info(title, 'skills', skills)